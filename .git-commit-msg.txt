chore: configure-pages 增加 enablement 参数尝试自动开启 Pages

GitHub 限制 GITHUB_TOKEN 无法代为开启 Pages 仓库设置，
因此首选方案仍是手动把 Settings → Pages → Source 设为 GitHub Actions；
此参数在部分情况下可自动完成，作为兜底。

同时本次 push 会重新触发部署（path 过滤已覆盖 workflow 文件本身），
用户无需在 Actions 页面手动点 Run workflow。
