/**
 * 运行模式配置
 *
 * 本文件由构建/启动流程生成（可能被 .gitignore 排除），用于区分两种形态：
 *   - 正常模式（npm start）：前后端一起跑，走真实后端 API
 *   - 静态演示模式（GitHub Pages）：没有后端，API 由浏览器本地实现
 *
 * 如果在 GitHub Pages 上打开后一直停在加载页，多半是本文件缺失或 BASE 不对。
 */
window.__CY_DEMO__ = {
  // true = 静态演示模式（浏览器本地数据 + 本地 API），false/缺省 = 使用真实后端
  mode: true,

  // 站点根路径：GitHub Pages 部署在子目录时必须是 '/仓库名/'，根域名部署用 '/'
  // 若不确定，可设为 './'（但站内绝对路径链接需要与之匹配）
  base: '/chayaya-mall/',

  // 构建时间戳，便于确认看到的是哪次构建
  builtAt: '__BUILT_AT__',

  // 说明文案（展示给使用者，避免误解为真实后端）
  note: '静态演示模式：数据为构建时快照，订单不会持久化，管理后台不包含在内。',
};
